---
description: (codepatrol) Load the project Build lifecycle skill
---
Load and follow the project skill `codepatrol-build` (`skills/codepatrol-build/SKILL.md`) with this request: $ARGUMENTS

Adapter context: harness is `claude`; use the actual current model ID, or `claude-current` when it is unavailable.
