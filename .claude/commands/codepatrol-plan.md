---
description: (codepatrol) Load the project Plan lifecycle skill
---
Load and follow the project skill `codepatrol-plan` (`skills/codepatrol-plan/SKILL.md`) with this request: $ARGUMENTS

Adapter context: harness is `claude`; use the actual current model ID, or `claude-current` when it is unavailable.
