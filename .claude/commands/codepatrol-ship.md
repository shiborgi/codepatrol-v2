---
description: (codepatrol) Load the project Ship lifecycle skill
---
Load and follow the project skill `codepatrol-ship` (`skills/codepatrol-ship/SKILL.md`) with this request: $ARGUMENTS

Adapter context: harness is `claude`; use the actual current model ID, or `claude-current` when it is unavailable.
