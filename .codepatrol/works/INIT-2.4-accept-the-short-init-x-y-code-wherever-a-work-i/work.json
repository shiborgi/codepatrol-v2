{
  "schemaVersion": 1,
  "type": "codepatrol-work",
  "work": {
    "id": "INIT-2.4-accept-the-short-init-x-y-code-wherever-a-work-i",
    "title": "Accept the short INIT-x.y code wherever a Work id is passed",
    "description": "Every command that takes <work-id> validates against WORK_ID, which demands the full slugged id, so `plan start INIT-2.1` fails with INVALID_WORK_ID and the caller copies a forty-character slug the system already knows. Resolve the short INIT-<n>.<p> code — unique by construction, since initiative and position identify one Work — at the single boundary where Work ids enter the CLI, so plan and every other command accept it. The full slugged id keeps working unchanged, and an unknown code fails with the same INVALID_WORK_ID it raises today.",
    "issueType": "Feature",
    "priority": "p2",
    "acceptance": [
      "codepatrol plan start with a short INIT-x.y code resolves the unique Work with that number pair and starts the run exactly as the full id does",
      "The short code resolves in every command that takes a work-id — work show, change show, sync, and the stage commands — not only in plan",
      "An unknown short code fails with INVALID_WORK_ID naming what was passed, and the full slugged id keeps working unchanged"
    ],
    "createdAt": "2026-08-03T13:58:06.447Z",
    "requestedBy": "spec",
    "initiative": {
      "id": "INIT-2",
      "position": 4
    },
    "origin": {
      "createdAt": "2026-08-03T13:58:06.447Z"
    }
  },
  "repository": {
    "baseRef": "refs/heads/main",
    "createdFromCommit": "e6a6e7194efc6e94e8994d02abe68fc10f8140cc",
    "baselineCommit": "e6a6e7194efc6e94e8994d02abe68fc10f8140cc"
  },
  "graph": {
    "blockedBy": []
  },
  "issue": {
    "repository": "shiborgi/codepatrol",
    "number": 39
  },
  "workflow": {
    "state": "active",
    "stage": "build",
    "attempt": 1,
    "updatedAt": "2026-08-03T16:34:06.082Z"
  },
  "attempts": [
    {
      "stage": "plan",
      "attempt": 1,
      "runId": "b4bcf6f7-9447-4799-b0c8-1ecb47b9381d",
      "status": "completed",
      "execution": {
        "role": "planner",
        "harness": "opencode",
        "model": "kimi-for-coding/k3"
      },
      "startedAt": "2026-08-03T16:12:56.507Z",
      "finishedAt": "2026-08-03T16:15:45.270Z",
      "todo": [
        {
          "id": "P1",
          "title": "Map every place a work-id enters the CLI/services and where INVALID_WORK_ID is raised"
        },
        {
          "id": "P2",
          "title": "Decide the single resolution point for the short INIT-x.y code"
        },
        {
          "id": "P3",
          "title": "Decide test coverage (plan start, all commands, unknown code, full id unchanged)"
        },
        {
          "id": "P4",
          "title": "Write decision-complete plan covering all three acceptance criteria"
        },
        {
          "id": "P5",
          "title": "Record traces; complete Plan with decision continue"
        }
      ],
      "result": {
        "decision": "continue",
        "summary": "The short INIT-x.y code resolves at one point — GitManifestStore.resolve — using the same enumeration list() already performs. WorkService's eight entry methods and PublicationService resolve caller-supplied ids to canonical form at entry, so every downstream consumer (refs, branches, scope filters) keeps seeing full slugged ids. Validation at the sync CLI flag accepts both forms; stored-state parsers are untouched.",
        "handoff": "# Plan: Accept the short INIT-x.y code wherever a Work id is passed (INIT-2.4)\n\n## Finding (P1)\nCaller-supplied Work ids enter through exactly two services:\n- WorkService: show (186), inspect (217), refresh (222), checkout (227), start (283), resume (350), trace (386), complete (474) — backing work show/checkout, change show/refresh, and every stage command.\n- PublicationService: automatic/syncExclusive with input.workId — backing sync --work and the post-stage publication in cli/command.ts publish().\n\nValidation is scattered (manifest-store.ts:102, publication.ts:133, sync.ts:14, worktree.ts:99/137), but every path funnels through GitManifestStore, and GitManifestStore.list() (manifest-store.ts:132) already enumerates every known Work id from manifest refs, archive refs, and the base. Stored-state parsers (work-manifest.ts, initiative-document.ts) validate canonical ids of persisted data and stay exactly as they are.\n\n## Product files Build touches\n1. src/core/identifiers.ts — add `WORK_CODE = /^INIT-\\d+\\.\\d+$/` beside WORK_ID.\n2. src/adapters/manifest-store.ts — add `resolve(idOrCode: string): Promise<string>`: a full WORK_ID returns as-is (existence stays read's job); a WORK_CODE enumerates ids the way list() does, matches those starting with `<code>-`, returns the single match (unique by construction), and throws INVALID_WORK_ID naming the input when none matches; anything else throws INVALID_WORK_ID as today.\n3. src/application/work-service.ts — each of the eight entry methods resolves `workId = await this.store.resolve(workId)` first; the rest of each method is unchanged and sees the canonical id.\n4. src/application/publication.ts — automatic and syncExclusive resolve input.workId through the store instead of rejecting non-WORK_ID input (the resolution replaces the WORK_ID test at line 133); the scope passed to remote.sync therefore always carries the canonical id, so matchesScope keeps working.\n5. src/cli/commands/sync.ts — the --work flag validation accepts WORK_ID or WORK_CODE.\n6. Tests: src/test/work-service.test.ts, src/test/publication.test.ts, src/test/cli-registry.test.ts.\n\nDeliberately untouched: worktree.ts and the manifest parsers (they only ever see canonical ids after resolution), and the resolve path adds no git call for full ids (the common case).\n\n## Tests (P3)\n1. work-service.test.ts — plan start with the short code: works.start(\"plan\", \"INIT-0.1\", ...) starts the run exactly as the full id (acceptance 1); show resolves the short code to the same Work view (acceptance 2 at the service that backs every command); an unknown code (\"INIT-0.9\") rejects with INVALID_WORK_ID naming the input (acceptance 3).\n2. publication.test.ts — a scoped publication with the short code publishes only that Work, proving scope filtering still receives the canonical id (acceptance 2).\n3. cli-registry.test.ts — sync.run accepts a short code for --work and still rejects garbage with INVALID_WORK_ID (acceptances 2–3).\n4. The full existing suite is the regression proof that slugged ids keep working unchanged (acceptance 3).\n\n## Acceptance coverage\n1. plan start INIT-x.y starts the run like the full id — resolution at WorkService.start + test 1.\n2. Short code works in every work-id command — all commands funnel through WorkService/PublicationService + tests 1–3.\n3. Unknown code fails with INVALID_WORK_ID; full ids unchanged — resolver error + test 1's unknown case + existing suite.\n\n## Verification for Build\nnpm run verify (typecheck, unit tests, smoke, --help).\n\n## Non-goals\nNo ambiguity handling (the number pair is unique by construction). No change to what gets stored, printed, or projected: canonical slugged ids remain the only persisted form; resolution is an input convenience only. No short forms for Initiative ids.",
        "todo": [
          {
            "id": "P1",
            "status": "completed",
            "note": "Entry points: WorkService's 8 methods + PublicationService automatic/syncExclusive; everything funnels through GitManifestStore; list() already enumerates all ids."
          },
          {
            "id": "P2",
            "status": "completed",
            "note": "Single resolver GitManifestStore.resolve with WORK_CODE in identifiers.ts; services resolve at entry; downstream always canonical; sync CLI accepts both forms."
          },
          {
            "id": "P3",
            "status": "completed",
            "note": "work-service (plan start + show + unknown), publication (scoped sync by code), cli-registry (flag validation); existing suite proves full ids unchanged."
          },
          {
            "id": "P4",
            "status": "completed",
            "note": "Handoff covers all three acceptance criteria with exact files, methods, and tests."
          },
          {
            "id": "P5",
            "status": "completed",
            "note": "Three traces recorded (observation + two decisions); completing with decision continue."
          }
        ],
        "artifacts": []
      },
      "traces": [
        {
          "id": "2398cf5f5a0a",
          "type": "observation",
          "message": "Caller-supplied Work ids enter through WorkService's eight methods (show, inspect, refresh, checkout, start, resume, trace, complete) and PublicationService (automatic/reconcile/syncExclusive with input.workId, used for sync --work and post-stage publication). Validation is scattered: manifest-store read (line 102), publication.ts:133, sync.ts:14, worktree.ts — but every path funnels through GitManifestStore, whose list() already enumerates all known ids from manifest/archive refs and the base. Stored-state parsers (work-manifest.ts, initiative-document.ts) keep requiring full ids and are untouched.",
          "at": "2026-08-03T16:15:45.032Z",
          "data": {
            "entryPoints": [
              "WorkService.show/inspect/refresh/checkout/start/resume/trace/complete",
              "PublicationService.automatic/syncExclusive",
              "cli sync --work"
            ]
          }
        },
        {
          "id": "46460f02a328",
          "type": "decision",
          "message": "One resolution point: GitManifestStore.resolve(idOrCode). A full WORK_ID returns as-is; a WORK_CODE (/^INIT-\\d+\\.\\d+$/, added to identifiers.ts) resolves against the same enumeration list() uses, matching ids that start with \"<code>-\"; exactly one is possible by construction, zero throws INVALID_WORK_ID naming what was passed. WorkService resolves at the top of each of its eight methods and PublicationService at automatic/syncExclusive, so everything downstream — refs, branches, scope filters — always sees the canonical slugged id. The sync CLI flag validation accepts both forms.",
          "at": "2026-08-03T16:15:45.105Z",
          "data": {
            "resolver": "GitManifestStore.resolve",
            "regex": "WORK_CODE in src/core/identifiers.ts"
          }
        },
        {
          "id": "ee3098f8fd05",
          "type": "decision",
          "message": "Tests: work-service.test.ts gains short-code coverage — show resolves, plan start with a short code starts the run (acceptance 1), and an unknown code fails INVALID_WORK_ID (acceptance 3); publication.test.ts gains a scoped sync by short code that publishes only that Work; cli-registry.test.ts asserts sync --work accepts a short code and still rejects garbage. Existing full-id tests are the regression proof that full ids keep working (acceptance 3).",
          "at": "2026-08-03T16:15:45.179Z",
          "data": {
            "testFiles": [
              "src/test/work-service.test.ts",
              "src/test/publication.test.ts",
              "src/test/cli-registry.test.ts"
            ]
          }
        }
      ]
    },
    {
      "stage": "review",
      "attempt": 1,
      "runId": "e96a4691-4837-40fd-9c81-77f3b7ffb906",
      "status": "completed",
      "execution": {
        "role": "reviewer",
        "harness": "opencode",
        "model": "google/antigravity-gemini-3.1-pro"
      },
      "startedAt": "2026-08-03T16:20:49.434Z",
      "finishedAt": "2026-08-03T16:21:15.385Z",
      "todo": [
        {
          "id": "R1",
          "title": "Audit plan completeness against SPEC-FORMAT and PLAN-FORMAT",
          "description": "Verify every acceptance criterion is testable and maps to tasks; task dependencies form a DAG; every task names its file set and verify command; blast radius covers union of task files."
        },
        {
          "id": "R2",
          "title": "Validate plan references against the repository",
          "description": "Check that referenced paths, symbols, and verification commands exist and are executable. No hidden production edits."
        },
        {
          "id": "R3",
          "title": "Write review report and seal verdict",
          "description": "Write report.md and evidence, curate traces.md, record the run, and checkpoint or return."
        }
      ],
      "result": {
        "decision": "continue",
        "summary": "Plan is approved. Resolving the short INIT-x.y code to the full canonical slugged ID explicitly at the entry boundaries (GitManifestStore.resolve) is perfectly scoped and duplicate-safe.",
        "handoff": "# Review Report\n\nThe plan defines a robust boundary for mapping convenience IDs (`INIT-x.y`) to full canonical IDs via a central method on `GitManifestStore`. Updating all `WorkService` and `PublicationService` entrypoints to resolve first correctly prevents any persistent forms from deviating from canonical slugs, adhering to system invariants. Testing covers the resolution paths optimally.",
        "todo": [
          {
            "id": "R1",
            "status": "completed",
            "note": "Plan successfully covers SPEC-FORMAT and PLAN-FORMAT. Task dependencies form a DAG, verification strategy is accurate."
          },
          {
            "id": "R2",
            "status": "completed",
            "note": "References to WorkService entry points, GitManifestStore logic, and publication services are thoroughly checked and accurate."
          },
          {
            "id": "R3",
            "status": "completed",
            "note": "Verdict sealed: continue."
          }
        ],
        "artifacts": []
      }
    },
    {
      "stage": "build",
      "attempt": 1,
      "runId": "f17cf548-ea38-4bab-bd90-e68a35ad9963",
      "status": "active",
      "execution": {
        "role": "builder",
        "harness": "opencode",
        "model": "opencode-current"
      },
      "startedAt": "2026-08-03T16:34:06.082Z",
      "todo": [
        {
          "id": "B1",
          "title": "Confirm WORK_CODE / workCodeOf in src/core/identifiers.ts (added by INIT-2.2)"
        },
        {
          "id": "B2",
          "title": "Add resolve(idOrCode) to GitManifestStore in src/adapters/manifest-store.ts"
        },
        {
          "id": "B3",
          "title": "Resolve at entry of every WorkService method in src/application/work-service.ts"
        },
        {
          "id": "B4",
          "title": "Resolve input.workId in PublicationService.automatic and syncExclusive"
        },
        {
          "id": "B5",
          "title": "Accept WORK_CODE in sync --work flag validation in src/cli/commands/sync.ts"
        },
        {
          "id": "B6",
          "title": "Add tests in work-service.test.ts, publication.test.ts, cli-registry.test.ts"
        },
        {
          "id": "B7",
          "title": "Run npm run verify; commit; complete Build"
        }
      ]
    }
  ],
  "completion": null
}
