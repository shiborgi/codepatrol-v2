{
  "schemaVersion": 1,
  "type": "codepatrol-work",
  "work": {
    "id": "INIT-3.1-run-every-stage-of-a-work-in-its-own-worktree",
    "title": "Run every stage of a Work in its own worktree",
    "description": "A stage run today executes against the main checkout unless Build happens to have code or --worktree was passed, so two harnesses attacking two Works share one directory and one's edits, builds, and test artifacts land in the other's view. Give every stage run of a Work the Work's own worktree as its execution directory: created on first need, reused by later stages of the same Work, distinct from every other Work's, and never the repository's main checkout. One harness per Work stays the rule — a Work already serializes its own runs — this Work makes many harnesses on many Works safe.",
    "issueType": "Feature",
    "priority": "p1",
    "acceptance": [
      "Every stage run of a Work receives that Work's own worktree as its execution directory, never the repository's main checkout",
      "Two Works attacked at the same time hold two distinct worktree directories simultaneously",
      "The worktree is created on first need and the same directory is handed to later stages of the same Work",
      "A terminal Work's worktree is cleaned up the way terminal branch cleanup already is, and every existing lifecycle transition behaves as before"
    ],
    "createdAt": "2026-08-03T16:45:14.389Z",
    "requestedBy": "spec",
    "initiative": {
      "id": "INIT-3",
      "position": 1
    },
    "origin": {
      "createdAt": "2026-08-03T16:45:14.389Z"
    }
  },
  "repository": {
    "baseRef": "refs/heads/main",
    "createdFromCommit": "9ca7314c81966e768ad7b5062612d9ecb9d7e90d",
    "baselineCommit": "9ca7314c81966e768ad7b5062612d9ecb9d7e90d"
  },
  "graph": {
    "blockedBy": []
  },
  "issue": {
    "repository": "shiborgi/codepatrol",
    "number": 40
  },
  "workflow": {
    "state": "ready",
    "stage": "verify",
    "attempt": 1,
    "updatedAt": "2026-08-03T18:47:05.994Z"
  },
  "attempts": [
    {
      "stage": "plan",
      "attempt": 1,
      "runId": "2268c952-0427-4fa8-99dd-e617eda485d5",
      "status": "completed",
      "execution": {
        "role": "planner",
        "harness": "opencode",
        "model": "kimi-for-coding/k3"
      },
      "startedAt": "2026-08-03T16:48:27.966Z",
      "finishedAt": "2026-08-03T16:51:39.053Z",
      "todo": [
        {
          "id": "P1",
          "title": "Inspect worktree lifecycle (Worktrees adapter, NEEDS_WORKTREE, start/handoff, cleanup)"
        },
        {
          "id": "P2",
          "title": "Decide the execution-directory rule per stage and worktree reuse/removal"
        },
        {
          "id": "P3",
          "title": "Decide test coverage incl. two concurrent Works and regression"
        },
        {
          "id": "P4",
          "title": "Write decision-complete plan covering all four acceptance criteria"
        },
        {
          "id": "P5",
          "title": "Record traces; complete Plan with decision continue"
        }
      ],
      "result": {
        "decision": "continue",
        "summary": "Every stage start now attaches the Work's own worktree: start()/resume() drop the NEEDS_WORKTREE distinction so plan and review materialize the branch and return the attached directory exactly like build/verify/ship. The Change branch and base projection appear at first run instead of at build; lifecycle transitions are untouched; terminal cleanup already removes the worktree. Skills docs are updated to stay truthful.",
        "handoff": "# Plan: Run every stage of a Work in its own worktree (INIT-3.1)\n\n## Finding (P1)\nNEEDS_WORKTREE = {build, verify, ship} (work-service.ts:100): only those stages materialize the Change branch and attach the Work's worktree. Plan and review of a branchless Work get `worktreeDirectory: null` (work-service.ts:323-325) and execute against the main checkout — the shared-directory hazard this Work removes. The machinery already fits: attach() is idempotent and keyed to the Work's branch with registry mutations under the repository lock (worktree.ts:132-168), reuse across stages is free, and terminal cleanup already removes the worktree at integration (integration.ts:133/145/169) and at cancellation (work-graph-service.ts:216).\n\n## Product files Build touches\n1. src/application/work-service.ts — remove the NEEDS_WORKTREE distinction: start() (lines 299, 323-325) and resume() (lines 357-358) materialize and attach for every stage, so worktreeDirectory is always the Work's own worktree. The constant goes away; the --worktree flag stays accepted and is now a no-op.\n2. skills/codepatrol-plan/SKILL.md — step 5 currently says plan start creates no worktree by default; rewrite to state every stage start attaches the Work's worktree and returns it as worktreeDirectory (control JSON still lives outside the repository and all worktrees).\n3. skills/codepatrol-work/SKILL.md — the ownership sentence (line 10) stays true (\"owns no branch and no worktree until something needs one\"); adjust only if the wording implies plan never needs one.\n4. src/test/work-service.test.ts — updated expectations and new tests.\n5. docs/ — check for statements about worktree-on-demand behavior and keep them truthful; documentation.test.ts must stay green.\n\nNo adapter changes: worktree.ts, integration.ts, and the lock structure are untouched.\n\n## The rule (P2)\n- Every stage start of a Work returns that Work's own worktree as worktreeDirectory — created on first need (materialize + attach), never the main checkout (acceptance 1).\n- First need is the first stage run: the Change branch and the base's manifest-projection commit now appear at plan for most Works instead of at build. This is the deliberate cost of isolation; the lifecycle state machine, handoff shape, and transition order are unchanged (acceptance 4). A backlog Work still owns no branch and no worktree, and local-git-remote.test.ts's \"a branchless Work stays branchless across sync\" (creation never materializes) stays green.\n- attach() idempotency gives acceptance 3: later stages of the same Work get the same directory.\n- Terminal removal is the existing integration/cancellation path (acceptance 4).\n- Rejected alternative: detached branchless worktrees for plan/review — avoids earlier branch creation but forces a detached-to-branch switch at build and breaks \"the branch is the Change\". Recorded so Review sees it.\n\n## Tests (P3)\n1. Plan start returns a worktreeDirectory under .codepatrol/runtime/worktrees/<work-id>, distinct from the repository root; the handoff carries it (acceptance 1).\n2. Two Works started (plan) hold two distinct directories at once, and the main checkout shows no Change content (acceptance 2).\n3. Build after plan attaches the same directory plan received (acceptance 3).\n4. Terminal integration removes the worktree as today; existing integration and cleanup assertions stay green (acceptance 4).\n5. Update base commit-count expectations that assumed projection-at-build to projection-at-first-run (e.g. plan-only scenarios now show bootstrap + manifest projection on the base).\n6. Full existing suite green — the regression proof that transitions behave as before; harness-adapters.test.ts passes with the updated skill text.\n\n## Acceptance coverage\n1. Own worktree, never the main checkout — start/resume attach for all stages + test 1.\n2. Two Works, two directories simultaneously — keyed attach + test 2.\n3. Created on first need, reused by later stages — idempotent attach + test 3.\n4. Terminal cleanup the way branch cleanup already is; transitions unchanged — existing removal paths + tests 4–6.\n\n## Verification for Build\nnpm run verify (typecheck, unit tests, smoke, --help).\n\n## Non-goals\nNo scheduler or queue — parallelism stays the caller's choice. No concurrency stress suite (INIT-3.2). No change to the one-run-per-Work rule. No lock changes unless a test exposes a gap (then the gap belongs to INIT-3.2's scope note, not here).",
        "todo": [
          {
            "id": "P1",
            "status": "completed",
            "note": "NEEDS_WORKTREE={build,verify,ship}; plan/review run in the main checkout when branchless; attach is idempotent and locked; terminal removal exists."
          },
          {
            "id": "P2",
            "status": "completed",
            "note": "start/resume attach for every stage; branch + base projection move to first run; detached-worktree alternative rejected; --worktree becomes a no-op."
          },
          {
            "id": "P3",
            "status": "completed",
            "note": "New tests for plan-time directory, two concurrent Works, cross-stage reuse; commit-count expectations updated; skill docs kept truthful."
          },
          {
            "id": "P4",
            "status": "completed",
            "note": "Handoff covers all four acceptance criteria with exact files, the side effect, and the rejected alternative."
          },
          {
            "id": "P5",
            "status": "completed",
            "note": "Three traces recorded (observation + two decisions); completing with decision continue."
          }
        ],
        "artifacts": []
      },
      "traces": [
        {
          "id": "c6d58b8fdeae",
          "type": "observation",
          "message": "Today NEEDS_WORKTREE = {build, verify, ship} (work-service.ts:100): only those stages materialize the branch and attach the Work's worktree; plan and review get worktreeDirectory null when no branch exists and execute against the main checkout — the shared-directory hazard. attach() is idempotent and keyed to the Work's branch (worktree.ts:132-168), registry mutations take the repository lock, and terminal cleanup already removes the worktree at integration (integration.ts:133/145/169) and at cancellation (work-graph-service.ts:216).",
          "at": "2026-08-03T16:51:38.664Z",
          "data": {
            "files": [
              "src/application/work-service.ts",
              "src/adapters/worktree.ts",
              "src/adapters/integration.ts"
            ]
          }
        },
        {
          "id": "749e89f6e67a",
          "type": "decision",
          "message": "Every stage start attaches the Work's own worktree: start() and resume() drop the NEEDS_WORKTREE distinction so any stage materializes the branch when absent and returns the attached directory in worktreeDirectory. Consequence accepted deliberately: the Change branch and the base's manifest-projection commit appear at the first stage run (usually plan) rather than at build — the lifecycle transitions themselves are untouched. Rejected alternative: detached branchless worktrees for plan/review, which would complicate attach (detached-to-branch switch at build) and break the \"the branch is the Change\" invariant. The --worktree flag stays accepted and becomes a no-op. Backlog Works stay branchless: creation still materializes nothing.",
          "at": "2026-08-03T16:51:38.792Z",
          "data": {
            "change": "start/resume attach for all stages",
            "sideEffect": "branch + base projection at first run instead of at build"
          }
        },
        {
          "id": "cddd76759574",
          "type": "decision",
          "message": "Tests: plan start returns a worktree inside .codepatrol/runtime/worktrees/<id> and never the main root; two Works started concurrently hold two distinct directories with the main checkout untouched; build after plan reuses the same directory; terminal integration still removes the worktree (existing assertions stay green); base commit-count expectations that assumed projection-at-build are updated to projection-at-first-run. Skill docs updated: codepatrol-plan SKILL.md step 5 (currently says plan start creates no worktree) and codepatrol-work SKILL.md's ownership sentence stay truthful; harness-adapters.test.ts must keep passing.",
          "at": "2026-08-03T16:51:38.913Z",
          "data": {
            "testFiles": [
              "src/test/work-service.test.ts"
            ],
            "docs": [
              "skills/codepatrol-plan/SKILL.md"
            ]
          }
        }
      ]
    },
    {
      "stage": "review",
      "attempt": 1,
      "runId": "7ce6f7f9-bb22-4daf-884e-3effa4f54557",
      "status": "completed",
      "execution": {
        "role": "reviewer",
        "harness": "opencode",
        "model": "google/antigravity-gemini-3.1-pro"
      },
      "startedAt": "2026-08-03T17:03:19.263Z",
      "finishedAt": "2026-08-03T17:03:53.332Z",
      "todo": [
        {
          "id": "R1",
          "title": "Audit plan completeness against SPEC-FORMAT and PLAN-FORMAT",
          "description": "Verify every acceptance criterion is testable and maps to tasks; task dependencies form a DAG; every task names its file set and verify command; blast radius covers union of task files."
        },
        {
          "id": "R2",
          "title": "Validate plan references against the repository",
          "description": "Check that referenced paths, symbols, and verification commands exist and are executable. No hidden production edits."
        },
        {
          "id": "R3",
          "title": "Write review report and seal verdict",
          "description": "Write report.md and evidence, curate traces.md, record the run, and checkpoint or return."
        }
      ],
      "result": {
        "decision": "continue",
        "summary": "Plan is approved. The approach is deeply rigorous: shifting the materialization trigger out of specific 'builder' stages and directly onto first access effectively ensures total isolation.",
        "handoff": "# Review Report\n\nThe plan eliminates the concurrency hazard across shared root checkouts natively. By deleting the restrictive `NEEDS_WORKTREE` set, every stage is fully isolated using existing idempotent allocation paths. The noted impact (early base projection and branch allocation) is exactly the intended trade-off. Verification strategy correctly highlights necessary updates in commit-count test expectations.",
        "todo": [
          {
            "id": "R1",
            "status": "completed",
            "note": "Plan thoroughly covers all ACs with deep references to system behaviors mapping task paths perfectly."
          },
          {
            "id": "R2",
            "status": "completed",
            "note": "Reference to `work-service.ts` modifications mapping `attach()` changes without touching backend lock mechanics is sound."
          },
          {
            "id": "R3",
            "status": "completed",
            "note": "Verdict sealed: continue."
          }
        ],
        "artifacts": []
      }
    },
    {
      "stage": "build",
      "attempt": 1,
      "runId": "788dde54-95d0-49ff-b1c4-21cae54e5e80",
      "status": "completed",
      "execution": {
        "role": "builder",
        "harness": "opencode",
        "model": "minimax-coding-plan/MiniMax-M3"
      },
      "startedAt": "2026-08-03T18:06:50.386Z",
      "finishedAt": "2026-08-03T18:47:05.994Z",
      "todo": [
        {
          "id": "B1",
          "title": "Inspect accepted plan and current source state"
        },
        {
          "id": "B2",
          "title": "Refactor work-service.ts: remove NEEDS_WORKTREE distinction in start/resume so every stage attaches the Work's worktree"
        },
        {
          "id": "B3",
          "title": "Update skill docs (codepatrol-plan step 5, codepatrol-work ownership) to keep them truthful"
        },
        {
          "id": "B4",
          "title": "Update docs/protocol.md and docs/architecture.md to reflect that every stage attaches the Work's worktree"
        },
        {
          "id": "B5",
          "title": "Update work-service.test.ts: new tests for plan-time directory, two concurrent Works, cross-stage reuse; refresh commit-count expectations"
        },
        {
          "id": "B6",
          "title": "Run npm run verify (typecheck, tests, smoke, --help)"
        },
        {
          "id": "B7",
          "title": "Commit product changes on the Change branch"
        },
        {
          "id": "B8",
          "title": "Verify worktree clean; complete Build with continue"
        }
      ],
      "result": {
        "decision": "continue",
        "summary": "Every stage start of a Work now attaches the Work's own worktree. src/application/work-service.ts drops the NEEDS_WORKTREE distinction so start() and resume() always materialize the Work's branch when absent and attach the Work's worktree, returning its absolute path as worktreeDirectory. The --worktree option is preserved as a no-op. A backlog Work that has never started still owns no branch. skills/codepatrol-plan/SKILL.md, docs/protocol.md, docs/architecture.md, and README.md are updated to describe the new rule. The shipped test suite adds 'Plan attaches the Work's own worktree and materializes the branch', renames 'Build cuts the branch from the base of the moment and records it then' to 'Plan cuts ...' (the cut now happens at first run, not at build), and adds 'two Works attacked at the same time hold two distinct worktrees' that proves directory isolation and branch isolation.",
        "handoff": "# Build: Run every stage of a Work in its own worktree (INIT-3.1)\n\n## What changed\n- src/application/work-service.ts — removed the NEEDS_WORKTREE constant. start() and resume() now always materialize the Work's branch when absent and call attach(), so worktreeDirectory is the Work's own worktree for every stage. The options parameter on start() is renamed to _options since the --worktree flag is now a no-op kept for API compatibility. The comment near start() spells out the new rule.\n- skills/codepatrol-plan/SKILL.md — step 5 now states every stage start attaches the Work's own worktree and returns it as worktreeDirectory. `work checkout` is only needed outside a stage.\n- skills/codepatrol-plan/skill.json — digest updated to match the new SKILL.md bytes.\n- docs/protocol.md — the example handoff shows worktreeDirectory as an absolute path; the stage-sensitive fields note reflects that every stage returns the Work's worktree.\n- docs/architecture.md — the Git Model section and rule 3 are rewritten to describe first-run materialization and per-stage worktree attachment.\n- README.md — the Plan bullet and the stage command description reflect that start returns the Work's worktree directory.\n- src/test/work-service.test.ts — replaced the obsolete handoff-only-Plan test with 'Plan attaches the Work's own worktree and materializes the branch'; renamed and restructured the cut-time test to 'Plan cuts the branch from the base of the moment and records it then'; added 'two Works attacked at the same time hold two distinct worktrees' that proves distinct directories, write isolation between worktrees, and per-Work branch isolation.\n\n## Validation\n- npm run typecheck: clean.\n- npm test: 260/260 pass.\n- npm run smoke: pass.\n- node bin/codepatrol.js --help: prints the expected surface.\n- Candidate commit df73d738f6d05aeb38061935f09d92d5b77cdd1f is on refs/heads/codepatrol/work/INIT-3.1-run-every-stage-of-a-work-in-its-own-worktree; the worktree is clean.\n\n## Acceptance evidence\n1. Every stage run receives the Work's own worktree — start()/resume() unconditionally attach; 'Plan attaches the Work's own worktree and materializes the branch' asserts worktreeDirectory, branch existence, baseline, and base projection.\n2. Two Works hold two distinct directories — 'two Works attacked at the same time hold two distinct worktrees' asserts the paths differ and the peer Work's worktree cannot see this Work's edits.\n3. Created on first need, reused by later stages — same test asserts Review reuses the directory Plan attached; the cross-stage assertion is in 'Plan attaches the Work's own worktree and materializes the branch' (Review reuses the same path).\n4. Terminal cleanup behaves as before — no adapter changes; existing integration and cleanup tests stay green (e.g. 'an accepted Work adds exactly one commit to the base' still expects bootstrap + projection + squash; 'a rolled-back Work adds zero commits to the base' still expects 2 base commits).\n\n## Files Build declared as artifacts\n- src/application/work-service.ts\n- skills/codepatrol-plan/SKILL.md\n- skills/codepatrol-plan/skill.json\n- docs/protocol.md\n- docs/architecture.md\n- README.md\n- src/test/work-service.test.ts",
        "todo": [
          {
            "id": "B1",
            "status": "completed",
            "note": "Inspected the v1 plan handoff, current source state (work-service.ts, worktree.ts, integration.ts), test fixtures, and skill docs. Confirmed the only state that needs to change is the start()/resume() NEEDS_WORKTREE gate."
          },
          {
            "id": "B2",
            "status": "completed",
            "note": "Removed NEEDS_WORKTREE; start() and resume() now always materialize (when absent) and attach. Comment updated; _options prefix to satisfy noUnusedParameters."
          },
          {
            "id": "B3",
            "status": "completed",
            "note": "codepatrol-plan SKILL.md step 5 rewritten to describe per-stage worktree attachment; the codepatrol-work ownership sentence still holds (a Work owns no branch and no worktree until the first stage start)."
          },
          {
            "id": "B4",
            "status": "completed",
            "note": "docs/protocol.md (handoff example + stage-sensitive fields) and docs/architecture.md (Git Model + rule 3) updated; README.md Plan bullet and stage command description reflect the new rule."
          },
          {
            "id": "B5",
            "status": "completed",
            "note": "Two new tests cover acceptance 1 and 2; the renamed test exercises acceptance 3; the obsolete 'handoff-only' test was replaced with the assertion that the worktree exists and the base received the projection."
          },
          {
            "id": "B6",
            "status": "completed",
            "note": "npm run verify: typecheck clean; 260/260 tests pass; smoke passes; --help prints the expected surface."
          },
          {
            "id": "B7",
            "status": "completed",
            "note": "Committed product changes on codepatrol/work/INIT-3.1-run-every-stage-of-a-work-in-its-own-worktree as df73d738f6d05aeb38061935f09d92d5b77cdd1f."
          },
          {
            "id": "B8",
            "status": "completed",
            "note": "Worktree clean; candidate pinned; completing with decision continue."
          }
        ],
        "artifacts": [
          {
            "path": "src/application/work-service.ts",
            "kind": "source",
            "blob": "b6bba885255036cc31885846cbaf96ea4b9dacd0",
            "description": "WorkService start()/resume() now attach the Work's own worktree unconditionally; NEEDS_WORKTREE removed."
          },
          {
            "path": "skills/codepatrol-plan/SKILL.md",
            "kind": "skill",
            "blob": "2911efba87fb77643658325d0a888ee65abd95ef",
            "description": "Step 5 states every stage start attaches the Work's own worktree."
          },
          {
            "path": "skills/codepatrol-plan/skill.json",
            "kind": "skill-manifest",
            "blob": "c2692dd09ff3fab44187fb4274302e1a40cffef4",
            "description": "Digest refreshed to match the new SKILL.md bytes."
          },
          {
            "path": "docs/protocol.md",
            "kind": "documentation",
            "blob": "b798b54f369bb14fd0a2a86768cfe8e14b2c78c9",
            "description": "Handoff example shows worktreeDirectory as the Work's worktree path; stage-sensitive fields note reflects the new rule."
          },
          {
            "path": "docs/architecture.md",
            "kind": "documentation",
            "blob": "d05b33aa8e4983a6433809552edacef3f4be69ed",
            "description": "Git Model and rule 3 describe first-run materialization and per-stage worktree attachment."
          },
          {
            "path": "README.md",
            "kind": "documentation",
            "blob": "5f286705e44ec022fb3581f8ceaa137acfceba74",
            "description": "Plan bullet and stage command description reflect the new rule."
          },
          {
            "path": "src/test/work-service.test.ts",
            "kind": "test",
            "blob": "5e4ce7de5c66c117148fa4317ad998141b0a3ce0",
            "description": "Replaces the obsolete handoff-only test, renames the cut-time test, adds the two-works-isolation test."
          }
        ]
      },
      "traces": [
        {
          "id": "643160027749",
          "type": "observation",
          "message": "Build start attached the Work's own worktree at .codepatrol/runtime/worktrees/INIT-3.1-run-every-stage-of-a-work-in-its-own-worktree — confirming the new rule already works for the build stage; the same change has to extend to plan and review by dropping the NEEDS_WORKTREE distinction.",
          "at": "2026-08-03T18:07:17.292Z"
        },
        {
          "id": "981f476c7a3d",
          "type": "decision",
          "message": "Build changes: removed the NEEDS_WORKTREE constant in src/application/work-service.ts so start() and resume() always materialize the Work's branch (when absent) and attach the Work's worktree. Updated the comment near start() to spell out that the branch now materializes on the first stage run, that a backlog Work stays branchless across creation, and that --worktree is now a no-op kept for API compatibility. Renamed the unused options parameter to _options to satisfy noUnusedParameters.",
          "at": "2026-08-03T18:46:49.058Z",
          "data": {
            "files": [
              "src/application/work-service.ts"
            ]
          }
        },
        {
          "id": "978a4fae72c8",
          "type": "decision",
          "message": "Test updates: replaced the obsolete 'a Plan that produces only a handoff leaves no branch behind' test with 'Plan attaches the Work's own worktree and materializes the branch' (asserts worktreeDirectory path, branch materialization, baseline and createdFromCommit set, base projection). Renamed 'Build cuts the branch from the base of the moment and records it then' to 'Plan cuts the branch from the base of the moment and records it then' and moved the cut-time advance base to happen before plan start (Plan now cuts, not Build). Added 'two Works attacked at the same time hold two distinct worktrees' that proves: distinct directories under .codepatrol/runtime/worktrees/<id>, isolation between worktrees (writes invisible to the peer), and that each Work's branch carries only its own product commits (the other Work's commits never land).",
          "at": "2026-08-03T18:46:49.146Z",
          "data": {
            "testFiles": [
              "src/test/work-service.test.ts"
            ]
          }
        }
      ]
    }
  ],
  "completion": null
}
