---
description: (codepatrol) Load the project Build lifecycle skill
---
Load and follow the project skill `codepatrol-build` with this request: $ARGUMENTS

Adapter context: harness is `opencode`; use the actual current model ID, or `opencode-current` when it is unavailable.
