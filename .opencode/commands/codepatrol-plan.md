---
description: (codepatrol) Load the project Plan lifecycle skill
---
Load and follow the project skill `codepatrol-plan` with this request: $ARGUMENTS

Adapter context: harness is `opencode`; use the actual current model ID, or `opencode-current` when it is unavailable.
