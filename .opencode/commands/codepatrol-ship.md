---
description: (codepatrol) Load the project Ship lifecycle skill
---
Load and follow the project skill `codepatrol-ship` with this request: $ARGUMENTS

Adapter context: harness is `opencode`; use the actual current model ID, or `opencode-current` when it is unavailable.
